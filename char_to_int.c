/*
#include <stdio.h>
#include <stdlib.h>

void char_to_int(char *s, int n)
{
  int len, i;

  len = sizeof(s)/sizeof(s[0]);

  for(i = 0; i < len; ++i)
    {
        n[i] = atoi(s[i]); // convert each element to int
    }
}
*/
